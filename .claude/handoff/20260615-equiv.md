[SESSION HANDOFF - 2026-06-15 #5 検証層深化]

== 追加実装（CPU検証済み・GPU不要） ==
- tsugi/equivalence.py: 数値等価性判定 compare(a,b,dtype)→EquivalenceReport
  + simulate_vendor_matmul(擬似ベンダー・テスト用シミュレーション)
  実証: 擬似NVIDIA(f32) vs AMD(f16累積) で max_abs=3.9e-2 をDIVERGENT検出
- tsugi/portability.py 拡充: 大K累積順序差・atomics差・fast-math差ルール追加
- tsugi/portcheck.py: `python -m tsugi.portcheck` で移植性レポートCLI
- test_equivalence.py 4件 + portability拡充 → 計25テストPASS / verify 12不変条件 / ruff clean

== 新視点の柱（完成） ==
1. portability(静的・実行前リスク) ✅ CPU
2. equivalence(数値等価性保証) ✅ CPU(検出ロジック)/ 実GPU比較は同compare()適用
3. portcheck CLI(製品面) ✅

== 次アクション ==
- 実GPU: 両ベンダー実行 → compare(nv_out, amd_out, "float16") で等価性照合
- portcheck: ユーザーカーネル(.py)読み込み実装(現状は内蔵デモのみ)
- portabilityルール追加: shared memバンクコンフリクト・占有率計算
- GPU codegen本体(別軸・要LLVM/MLIR+実機)

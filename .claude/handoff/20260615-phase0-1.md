[SESSION HANDOFF - 2026-06-15]

完了済み:
- Phase 0 完成形ファイル全納品: docs/SPEC.md, docs/ARCHITECTURE.md,
  docs/adr/ADR-001..004, README.md, docs/FAQ.md, docs/BENCHMARK.md
- Phase 1 骨格: CMakeLists.txt, tsugi.tile/tsugi.gpu dialect (TableGen),
  VendorLowering.cpp (skeleton), torch backend (skeleton), correctness harness,
  CI (ci.yml), LICENSE(Apache-2.0), SECURITY/CHANGELOG/CONTRIBUTING相当, pyproject
- 出荷スキャン: 課金コードなし/PIIなし/バイナリCUDA変換なし ✓
- ruff clean / py構文OK / correctness harness は GPU無しで正直にskip

進行中:
- Phase 1 PoC本体（単一matmulをNVPTX/AMDGPU両経路で実コンパイル・動作確認）
  次アクション: 要 LLVM/MLIR(NVPTX+AMDGPU有効) + NVIDIA/AMD実機。
  src/tsugi/backend/VendorLowering.cpp の TODO(Phase1) を実装。

ブロック中:
- 実GPU検証: 開発sandboxにLLVM/MLIR/torch/GPU無し。実機環境が必要。

既知の地雷:
- R1(行列コア抽象)が最難関。Phase2のDoD(cuBLAS比70-80%)に届かない場合は
  attention-onlyへスコープ縮小 or ADR-004再設計(G4ソクラテス問答)。
- shのブレース展開が効かない環境。mkdirは個別指定で。

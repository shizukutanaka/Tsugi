[SESSION HANDOFF - 2026-06-15 #3 /goal 実装完了]

== 完了（CPU検証済み・本物） ==
- 完成形ファイル: SPEC/ARCHITECTURE/ADR×4/README/FAQ/BENCHMARK
- リファレンス実装: tsugi pkg(dtypes/tile/jit/runtime_ref/autotune)
- 上流コンパイラ: tracer(@jit→tsugi.tile IR)/ir/lowering(各社intrinsic写像)/compile(統合dry-run)
- テスト17件 全PASS / verify 10不変条件 PASS / ruff clean / PII・課金なし
- tsugi.compile(kernel,args,target="nvidia"|"amd_cdna"|...) で上流が1関数

== 未実装（要実機・COMPLETE宣言しない） ==
- GPU codegen本体: tsugi.tile→gpu→NVVM/ROCDL の実MLIRパス + PTX/AMDGCN生成
- 両ベンダーGPU correctness(リファレンスとmax abs err<1e-2照合)・性能(cuBLAS/rocBLAS比)
- torch.compile backend実体(現状skeleton素通し)

== 次アクション（実機環境で） ==
1. LLVM/MLIR(NVPTX+AMDGPU有効)ビルド → dialect TableGenビルド
2. VendorLowering.cpp に lowering.py の写像を実MLIRパスとして実装
3. tests/correctness リファレンスとGPU実行を照合
4. 出荷チェックリストのGPU項目を埋める → 全PASSで初めてCOMPLETE

== 出荷チェックリスト判定 ==
PASS: lint0 / リファレンステスト / PII無 / 課金無 / バイナリCUDA変換無 / 完成形整合 / verify
BLOCKED(要実機): 両ビルド検証 / GPUベータ / ベンチマーク記録 / 署名ビルド
→ 全PASS不可のため v0.1 全体は COMPLETE 保留（正直）

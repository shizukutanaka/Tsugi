[SESSION HANDOFF - 2026-06-15 #7 /loop]

== ループ実施（ルーブリック駆動・1反復で収束） ==
改善: occupancy HW定数を推測値→一次情報源実値に置換
  - NVIDIA H100/Hopper: Hopper Tuning Guide (228KB smem/65536 regs/64 warps)
  - AMD MI300X/CDNA3: ROCm gpu-arch-specs (64KB LDS/131072 regs/32 wavefronts)
  - AMD RX7900XTX/RDNA3: GPUOpen (98304 regs/64KB LDS/32 waves)
  - docs/SOURCES.md に出典明記
  - 実値で NVIDIA 50%(reg制約) / AMD CDNA 25%(LDS制約) と現実化
検証: 33テストPASS / verify 13/13 / ruff clean 維持

== 収束判定（正直） ==
残候補=shared memバンクコンフリクト検出 → 現IRの粒度(load offsetが粗い)では
低シグナル。追加は「サイズ増のためだけの改善」になる(ハーネス禁止事項)→不採用。
ループ停止。これ以上のCPU改善は限界効用が低い。

== 次の実価値は実機軸（CPUループでは出せない） ==
- GPU codegen本体(tsugi.tile→gpu→NVVM/ROCDL実MLIRパス・要LLVM/MLIR+実機)
- 実GPUでequivalence compare()を両ベンダー出力に適用
- occupancy granularity(割当粒度)モデル化は実機計測と突き合わせて初めて価値

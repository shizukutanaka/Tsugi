[SESSION HANDOFF - 2026-06-15 #11 統合ファサード audit]

== 統合(視点でなく統合・3ハンドオフ越しの保留を解消) ==
8視点+メタ(calibration)+基盤(nondeterminism)+翻訳(decision)が揃い、これ以上の
新視点より統合が筋と判断。tsugi.audit が検証層を1つの判定に束ねる。

== 実装済み(CPU検証済み) ==
- tsugi/audit.py:
  - audit(module, cfg, targets, block_dims) -> Audit
  - 静的層をまとめて回す: portability(per target)/feasibility/occupancy/numerics
    (tolerance導出+envelope認証+calibration検出限界)
  - AuditPhase(name, when="static"|"runtime", max_risk, lines)
  - Audit.max_risk/portable は static_phases のみから(実機待ち層は判定に含めない)
  - runtime層=実行時チェックリストとして明示(envelope.check_tensor/
    nondeterminism.compare_stable/decision.compare_decisions)
  - to_text: 検証ライフサイクル(静的→動的→メタ→基盤→翻訳)を一望
  - K=dot反復×cfg.block_k(マジックナンバー排除)
- portcheck.report は audit へ委譲(アドホック統合グルー除去=DRY)。
  cross_vendor_diff(per-pair差分・auditに無い)だけ手前で併記。
  portcheck契約(起動不能/BLOCK/導出許容の文字列)維持
- __init__: tsugi.audit(関数)/tsugi.Audit 公開
- 統合: verify不変条件2件(31→33)
- test_audit.py 5件。計86テストPASS/verify33/ruff clean

== 検証の全体像(8視点+メタ+基盤+翻訳+統合) ==
audit が束ねる:
  静的: portability/feasibility/occupancy/tolerance/envelope(認証)/calibration(床)
  実行時チェックリスト: envelope.check_tensor/nondeterminism/decision
判定は静的層のみ(実機データが要る層は実機まで未確定)

== 次アクション ==
- 実機: run_fnを実GPUカーネルにしてnoise_floor実測→audit実行時層を実データで埋める
- audit に propagation(モデルグラフ伝播)を組込む(現状はper-kernel静的層のみ)
- GPU codegen本体(MLIR→PTX/AMDGCN)=要LLVM/MLIR+実機(未着手・lowering.pyが仕様)
EOF
echo done
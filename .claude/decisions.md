# Tsugi 判断ログ（decisions.md）

[2026-06-15] プロダクト名: Tsugi (継ぎ — GPU ベンダーを接合する移植検証層): ソクラテス問答で「多様性・おまかせ」を多層構成と解釈。
[2026-06-15] 中核IR=MLIR / NVIDIA経路=PTX / SPIR-V=フォールバック限定: ADR-001。SPIR-VはNVIDIAでCUDA計算first-classでない。
[2026-06-15] バイナリCUDA変換=不採用: ADR-002。NVIDIA EULA・ZLUDA撤回前例。法的リスク回避最優先。
[2026-06-15] 楔=torch.compileバックエンド優先: ADR-003。API競合せずPyTorchエコシステム継承。
[2026-06-15] 行列コア抽象=MLIR intrinsic経路・Vulkan coopmat非依存: ADR-004。最重要リサーチベット(R1)。
[2026-06-15] スコープ=NVIDIA+AMD・カーネル4系統・推論優先: ソロでCUDA全面複製は非現実的。縦に狭く深く。
[2026-06-15] ライセンス=Apache-2.0: 依存(LLVM/IREE)がApache-2.0系のため整合。
[2026-06-15] 環境制約: 開発sandboxにLLVM/MLIR/torch/GPU無し。Phase0(完成形ファイル)完納。Phase1は構造骨格まで。実GPU検証は要実機(主張と実装の一致)。
[2026-06-15] リファレンス実装(CPU/NumPy)を先行実装: GPU無し環境でも「正しさの真値」を確立。OpenCL失敗の解毒剤=リファレンス先行(リサーチ由来)。GPU codegenはこれと一致させる。8テストPASS。
[2026-06-15] tracer + lowering plan 実装: @tsugi.jit→tsugi.tile IR(具体トレース)→各社intrinsic写像。MLIR風テキスト出力。frontend→IR→vendor-map が本物に。14テストPASS。GPU codegen本体(MLIR実コンパイル)のみ実機残作業。
[2026-06-15] /goal 実装完了 → CPU検証可能スコープを完成締め。tsugi.compile(上流統合dry-run)・verify.py(10不変条件)追加。17テストPASS。GPU codegen本体はCOMPLETE宣言しない(実機未検証・主張と実装の一致)。出荷チェックリストはGPU項目がblockedのため全PASS不可→COMPLETE保留。
[2026-06-15] ソクラテス問答で新視点発見: Tsugiの最強の楔はcodegenでなく「クロスベンダー検証層」。Tritonは両ベンダー生成するが数値等価性を保証しない。堀の本質=ライブラリ+QA(SemiAnalysis: AMDの弱点はQA文化)。portability.py(移植リスク静的解析・GPU不要)を即実装。同一カーネルblock=32がNVIDIA OK/AMD WARNを検出。21テストPASS。→ GPU codegen完成を待たず最初の実用価値が出る。
[2026-06-15] 新視点を深化: equivalence層(数値等価性判定)+portability累積順序ルール+portcheck CLI実装。擬似ベンダー(f32 vs f16累積)でmax_abs=3.9e-2の発散をDIVERGENT検出を実証。25テストPASS/verify12。GPU codegen未完でもportcheck CLIが今すぐ実用価値を出す。
[2026-06-15] 検証層さらに深化: occupancy推定(ベンダー別占有率・同一構成m64n64k32s3w4でNVIDIA25%/AMD CDNA20%差)+portability占有率ルール+portcheckユーザーカーネル読込実装。examples/user_kernel.py(block48)で両ベンダーWARN検出を実証。33テストPASS/verify13。
[2026-06-15] /loop: occupancy HW定数を推測値→一次情報源実値に置換(NVIDIA Hopper Tuning Guide / AMD ROCm gpu-arch-specs / GPUOpen)。docs/SOURCES.md作成。実値でNVIDIA 50%(reg制約)/AMD CDNA 25%(LDS制約)。33テストPASS維持。ループ収束判定: 残候補(バンクコンフリクト検出)は現IR粒度で低シグナル=サイズ増のみ→不採用(ハーネス: サイズ増のためだけの改善はしない)。
[2026-06-15] ソクラテス問答2で新視点: equivalenceの固定許容(1e-2)が恣意的。両ベンダーは累積順序違いで両方IEEE正当→「真値に一致」でなく「数学が許す範囲」。tolerance.py(√K·u·safety導出)+compare_gemm実装。K=2048で固定1e-2が正当なf32順序差を過剰検出していたのを解消。39テストPASS/verify14。
[2026-06-15] /loop 2反復で収束: (1)bf16忠実丸め実装(従来bf16→f32マップで精度損失を無視していた弱点修正・round_to_bf16でround-to-nearest-even・tolerance u=2^-8と整合) (2)portcheckに導出許容目安を統合(検証層を1レポートに)。45テストPASS/verify14。次候補は実機noise_floor実測=CPU不可→収束。
[2026-06-15] ソクラテス問答3で新視点: occupancy/portabilityの前提を問い、occ=0%(起動不能)を「性能WARN」と誤分類していた盲点を発見。「占有率が低い(遅い)」と「そもそも起動できない(動かない)」は連続と離散で別物。per-block上限(smem/LDS NVIDIA227KB vs AMD64KiB等)を越える構成はlaunchすらしない=単一ソース約束の破綻。feasibility.py(起動可否のcategoricalゲート)実装+portability.analyzeを修正(起動不能はWARNでなくBLOCK)+portcheckに起動可能性セクション。同一構成m128n128k64s4w8がNVIDIA起動可/AMD起動不能を実証。検証順序=feasibility(動くか)→equivalence(正しいか)→occupancy(速いか)。51テストPASS/verify17。docs/PERSPECTIVE-launch-feasibility.md。
[2026-06-15] ソクラテス問答4で新視点: 既存検証層(equivalence/tolerance/feasibility/portability)は全て単一カーネルを判定していた盲点。だがユーザーはtorch.compile(model)でopグラフをコンパイルする。per-kernel等価⇏per-model等価—発散はグラフを伝播し深さで累積・ill-conditioned op(相殺reduction/小値除算/大exp)で増幅。propagation.py(δ_out=amp·δ_in+local の合成)実装。numpy実証: 累積順序違いの2ベンダーをmatmul+rmsnormの鎖に流すと発散が1→12層で約2000倍。モデル許容は単一カーネル許容の12倍。検証単位をカーネル→モデルへ。検証順序にpropagation(モデルで一致するか)を追加。56テストPASS/verify19。docs/PERSPECTIVE-error-propagation.md。
[2026-06-15] ソクラテス問答5で新視点: 既存4視点は全てデプロイ前の静的検証。等価性はscale/cond/Kの前提下で認証されるが、本番はoracleも第2ベンダーも無くAMD単体で未知入力が流れる。発散はデータ依存(propagation)→本番入力が認証エンベロープを逸脱すると静的保証が静かに無効化。envelope.py実装: 認証済み動作範囲(Envelope)+実行時検査(check_tensor/check_softmax_input)を単一ベンダー・oracle不要で。fp16 overflow(max65504)・denormal(FTZでベンダー差)・scale逸脱(認証atol無効化)・logit>ln(65504)≈11.09でexp inf破綻を検出。np.exp(fp16(12.5))が実際にinfになることを確認(机上でなく実挙動)。dtype別: fp16は範囲狭くoverflowが主リスク/bf16は範囲広くprecisionが主リスク。design-by-contractを数値に適用=静的証明の契約化。portcheckに認証エンベロープ併記。検証の時間軸を静的→動的へ拡張。63テストPASS/verify22。docs/PERSPECTIVE-runtime-envelope.md。
[2026-06-15] 現段階の長所短所棚卸し→改良(視点追加でなく統合・リファクタ): 長所=主張と実装の一致/CPU検証可能スコープ完成/8検証層が実用価値。短所=(1)検証層が乱立し各Reportが Risk/Finding/max_risk/to_text を重複実装(5箇所) (2)portcheckのK推定が n_dots*32 のマジックナンバー (3)production docstringに「第Nラウンド/新視点N」の開発過程ノイズ。改良: report.py(Risk+Finding+FindingReport基底)を新設し検証層横断で深刻度モデルを統一・portability/envelopeを移行(重複排除・DRY/Martin)。portcheckのKをcfg.block_k由来の見積りに修正(マジックナンバー除去)。touchした2モジュールのdocstringから開発過程ノイズを除去。63テストPASS/verify22維持・ruff clean。次の改善は9個目の視点でなく既存層の統合API(tsugi.audit)が筋。
[2026-06-15] ソクラテス問答6で新視点: 既存8層は全て「カーネル/モデルを検証する」が検証器自身は未検証だった盲点。検証器の最大の罪=偽OK(発散を等価と誤判定)→オラクル無きもう片方のベンダーにsilent誤りを出荷=検出不能で致命。偽BLOCK(回復可能)とコスト非対称→不確実ならBLOCK寄り(fail-safe)。許容ベース等価判定には検出限界floor=safety√K·u(相対)があり、これ未満のバグは原理的に不可視。floorはKで拡大(fp16:256→3.1%/2048→8.8%/8192→17.7%)=視点2(導出許容)が大K偽BLOCKを消した双対コストとして偽OK盲点を√Kで拡大していた。実証: 0.5%系統スケール誤差をmax_abs単独は全Kで見逃す(偽OK 3/6)。救済=scale/K不変な相補計量RMS比(乱雑発散はzero-mean/系統バグは相関)。is_equivalent_combinedでmax_abs+系統の合成判定→コーパス偽OK 0/6=TRUSTWORTHY。calibration.py実装(検証器を検証するメタ層)。検証器をassertionsから測定されたinstrumentsへ。9個目のsiloed視点でなく既存等価層を較正・補完するメタ層(前回の統合方針とも整合)。69テストPASS/verify25。docs/PERSPECTIVE-verifier-calibration.md。
[2026-06-15] ソクラテス問答7で新視点: 既存6視点+較正メタ層は全て「同じカーネルを同じベンダーで走らせれば同じ結果」=決定論を暗黙仮定していた盲点。GPUのatomic加算(split-K atomicAdd)はスレッド到着順で和の順序が変わりrun-to-runで揺れる→ベンダー出力は点でなく分布。tolerance.pyのnoise_floor既定0はこの決定論仮定の化石。単一run A vs 単一run Bは(a)ベンダー内ノイズと(b)ベンダー間発散を混同→食い違いをattribute不能・「Aの正しい答え」が一意に定義できない。クロス差がノイズ未満なら「AvsB」は「AvsA(別run)」と区別不能=第三状態INDISTINGUISHABLE(判定原理的に未定義)。視点6の検出限界(数値の床safety√Ku)とは独立した第二の床(HWの床)。実効分解能=max(数値検出限界,ノイズフロア)。ノイズが数値許容超でノイズ律速→偽BLOCK/偽OK。nondeterminism.py実装: simulate_nondeterministic_reduction(atomic擬似)/measure_noise_floor(複数run実測)/attribute(3状態帰属)/compare_stable(分布として健全比較). 実証: 真に等価な2ベンダーの単一run比較が観測差中間の許容でEQ/DVにフレーク。クロス差7.6e-5<ノイズ3.4e-4でINDISTINGUISHABLE。真の発散(5%底上げ)はノイズ超でDIVERGENT検出。GPU数値検証は等価/発散の二値でなくノイズ床明示の三値が正直。実機ではrun_fnを実カーネルにするだけ(CPUはatomic非決定の擬似・明示)。75テストPASS/verify28。docs/PERSPECTIVE-nondeterminism.md。
[2026-06-15] ソクラテス問答8で新視点: 全7視点は「数値がどれだけ違うか」(abs/rel/bias/noise=テンソル空間)を測ってきた盲点。だが開発者が出荷するのはタスクの判断(分類argmax/LM選択トークン/しきい値)。数値発散と判断はdecouple: (1)スケール不変=logit10倍でabs誤差10倍でもargmax不変→フリップ率同一(max_absは判断を測れない) (2)数値等価⇏タスク等価=巨大発散でもマージン大ならフリップ0/微小発散でもnear-tieならフリップ。判断フリップを支配するのは局所マージン(top1-top2)。発散δが判断を覆すにはmargin<2δが必要(十分でない)→フリップ率≤P(margin<2δ)という保守的上界。検証の最終単位はテンソルでなく決定。数値の床(視点6 √Ku)もHWの床(視点7ノイズ)もタスク影響に翻訳して初めて意味を持つ→橋=マージン分布。decision.py実装: margin/flip_rate/predicted_flip_bound/compare_decisions(タスク予算で判定・near-tie裾外フリップ=系統発散疑いを警告). 実証(2万サンプル1000クラス): eps振りでフリップは低マージン裾に集中(フリップ群中央値≪全体)、logit10倍でフリップ率同一、P(margin<2δ)が実フリップ率の上界(0.24%≤0.76%/2.60%≤8.98%/23.86%≤60.81%)、max_abs0.37の数値発散でもマージン大ならフリップ<0.5%。デプロイ判断は数値atolでなくタスク予算(フリップ率<0.1%等)を使う。81テストPASS/verify31。docs/PERSPECTIVE-task-equivalence.md。
[2026-06-15] 統合実装(視点が出揃ったので統合・3ハンドオフ越しの保留を解消): 8視点+メタ(calibration)+基盤(nondeterminism)+翻訳(decision)が揃い、これ以上の新視点より統合が筋と判断。tsugi/audit.py実装: traced IR+構成から静的に回せる層(portability/feasibility/occupancy/tolerance/envelope/calibration)をまとめて回し1つのAudit判定に集約(深刻度集約は単一責任)。実機データが要る層(envelope.check_tensor/nondeterminism.compare_stable/decision.compare_decisions)は実行時チェックリストとして明示し判定からは除外(静的層のみでverdict)。検証ライフサイクル(静的→動的→メタ→基盤→翻訳)を一望できるto_text。K=dot反復×block_k(マジックナンバー排除)。portcheck.reportはauditへ委譲しアドホックな統合グルー(envelope/tolerance/calibration個別print)を除去=DRY。portcheck契約(起動不能/BLOCK/導出許容の文字列)は維持。86テストPASS/verify33/ruff clean。次は実機(run_fnを実カーネルにしてnoise_floor実測→audit実行時層を実データで埋める)。
[2026-06-15] audit に propagation を統合(per-kernel→per-model): auditは静的per-kernel層のみだった。_graph_ops(module,cfg)でtraced IRを論理op列へ写像(K ループのdot群=load等で分断されるがmemory opはスキップして1 matmul K=反復×BKに集約・reduce/exp等は増幅op・cast/add等はscale)。torch.compile(model)のopグラフを模す。propagateでモデル発散 vs naive per-kernel和を出し、ratio>2でWARN(深さ/増幅でper-kernel等価⇏per-model等価)。単一matmulデモはratio=1=INFO(伝播増幅なし・正直)。cond静的不明ゆえ既定1(実機/モデルで上書き前提)。88テストPASS/verify34/ruff clean。これでaudit静的層=portability/feasibility/occupancy/numerics/propagation。残りpropagation以外の全視点が1レポートに。
[2026-06-16] /loop おまかせ: audit_runtime 実装(実行時チェックリストの実行版)。静的audit()は実機データ層をテキストのチェックリストにしていたが、実データがあれば実行できる鏡像 audit_runtime(a_out,b_out,K,env,noise_floor,logits_a,logits_b,flip_budget) を追加。与えられたデータに応じて適用層だけ回す: envelope.check_tensor(両出力が認証前提内)/equivalence(compare_gemm)+nondeterminism.attribute(noise込み3状態:DIVERGENT/EQ/INDISTINGUISHABLE)/calibration.check_systematic(max_abs盲点の系統バイアス)/decision.compare_decisions(タスクフリップ)。全て実データで決定済みゆえwhen=staticでverdictに算入。実証: 真に等価(微小差)+noise床→portable、5%系統スケール誤差→BLOCK、フリップ率予算超→BLOCK。CPUでシミュレートデータで検証可能、実機ではrun_fn/実出力を渡すだけ。91テストPASS/verify36/ruff clean。これでaudit=静的(audit)+実行時(audit_runtime)の両facade完備。残りは実機(GPU codegen本体+実出力でaudit_runtimeを駆動)。
[2026-06-16] /loop おまかせ(2): examples/audit_demo.py 実装(両facade一望の実行可能デモ・主張と実装の一致)。静的audit(AMD起動不能BLOCK)→実行時audit_runtime(健全AMD=portable / 0.5%系統バグAMD=BLOCK)を1スクリプトで実演。GPU不要・実機ではsimulate_vendor_matmulを実出力に置換するだけ。デモが「max_abs はEQ(cross0.329<atol0.498)だが系統バイアス0.5%で BLOCK」を実演=視点6の盲点を end-to-end で可視化。スモークテスト追加(主張と実装の一致を担保)。92テストPASS/verify36/ruff clean(examples含む)。README Usage にデモ併記。注: /loop dynamic mode の ScheduleWakeup がこの環境に無く自動ループ継続は不可(Bash sleep代用は禁止)。各イテレーションは手動 /loop 再invokeで継続。
[2026-06-16] /loop おまかせ(3): docs/VERIFICATION.md 作成(検証層の全体マップ)。8つのPERSPECTIVE docが個別に在ったが索引が無かった→ライフサイクル図(静的feasibility→portability→occupancy→tolerance/envelope→propagation / 動的envelope.check_tensor→nondeterminism→decision / メタcalibration)、8視点+メタの表(各docへリンク)、2つの床(数値safety√Ku[視点6]・HWノイズ[視点7]・実効分解能=max)、相補的計量(max_abs乱雑/RMS比系統/flip率タスク・fail-safe合成)、使い方を1枚に。READMEアーキ節からリンク。全8リンク解決確認・suite緑(verify36)。ナビゲーション改善のみ(コード不変)。
[2026-06-16] つづけて実装: 実機経路 audit_cross_vendor + tests/gpu ハーネス。nondeterminism(ノイズ実測)とaudit_runtime(ノイズを床に)をつなぐ正規経路が欠けていた→audit_cross_vendor(run_a,run_b,K,...): 各ベンダーをn_runs回走らせmeasure_noise_floor→max noise→audit_runtime。決定論を仮定しない実機入口。run_a/run_bはseed→出力テンソルのcallable(実機=実カーネル/CPU=擬似run)。tests/gpu/harness.py(run_vendor_kernelはNotImplementedError=codegen未実装を明示・audit_two_vendorsが配線)、test_audit_runtime_contract.py(GPU無しはSKIPだがハーネス配線を擬似runでCPU検証=契約を常に実行可能に保つ)。run.py [3]に統合(GPU無しでもALL PASS)。実証: 等価ベンダー→portable/5%系統バグ→BLOCK。94テストPASS/verify38/ruff clean。これでCPUで組める実機準備が完了(実機ではrun_vendor_kernelを実装しrun_a/run_bに渡すだけ)。残りは純粋にGPU実機作業(codegen本体+実カーネル実行)。
[2026-06-16] ソクラテス50問で改善点洗い出し+最優先を即修正。docs/SOCRATIC-50-improvements.md(50問・10テーマ・実コード観察ベース・P0/P1/P2優先度)。最大の構造的発見=B群(Q9/Q10): tracerがreduce/exp/sqrt/rsqrt/maximum等を一切IRに記録していなかった→softmax/rmsnormはトレース不能・auditのpropagationは常に単一matmulで空回り(目玉のper-kernel⇏per-model増幅が製品経路で発火せず)。根因修正: SymTensorにelementwise(add/sub/mul/div)、_TraceTileにreduce/exp/sqrt/rsqrt/maximum追加・trace()でpatch。softmaxがトレース可能に・増幅op(reduce/exp/div)がIRとaudit graphに流れる。検算: 具体トレース実値が真softmaxと一致。残P1=cond推定(増幅の大きさ・Q8)。併せてpropagation→decision橋(flip_bound_from_divergence: 静的相対発散→logit→P(margin<2δ)上界)も実装しaudit(ref_logits=)に併記=第2ベンダー前にタスク影響予測(視点4→8)。98テスト/verify38/ruff clean。他のP0(定数集約Q2/when改名Q45/SKIP件数表示Q37)とP1群は未着手(docに記録)。
[2026-06-16] つづけて: SOCRATIC Q8 修正(静的cond推定を正直に)。tracer修正で増幅opがaudit graphに流れるようになったが、static cond=1のままで増幅が発火しなかった。数値解析で「相対誤差の増幅op」を実測特定: reciprocal/div/addは相対条件数~1で増幅しない(絶対では効くが相対枠組みでは無害)、真の相対増幅は符号付きreductionの相殺(κ=Σ|x|/|Σx|)とexp(相対cond=|x|)のみ。対応: (1)_AMPLIFYINGを{reduce,softmax,exp}に是正+is_amplifier()。(2)empirical_cond(sample,kind,axis,reduce_kind): データ依存condを実測(reduce sum=Σ|x|/|Σx|・reduce max=1・exp=max|x|・他=1)。(3)audit propagationは増幅opがあるのに静的cond=1の時WARN+「下界・empirical_cond/audit_runtimeで定量化」と明記=過小評価を隠さない正直さ。実測確認: 符号付きreduce κ=61/正の和κ=1/exp=max|x|/div=1。既存propagationテスト(cond明示)は不変で緑。102テスト/verify40/ruff clean。残P1: torch backendに静的audit差込(Q23/26)・橋の系統/乱雑分離(Q18/20)・equivalenceをreport基盤へ(Q44/47)。
[2026-06-16] 最新論文を探索し取り込み。WebSearch で2024-2025の関連研究を調査(WebFetchは403で要約はsearch由来)。採用2件: (1)バッチ不変性=LLM推論の支配的非決定源(Thinking Machines 2025/SC'24 arXiv:2408.05148): あるサンプル出力がforwardのバッチサイズに依存(バッチ依存タイル/縮約順序で丸めが変わる)。atomic並行性ではない。nondeterminism.pyに simulate_batch_variant_reduction(parts,tile)/measure_batch_variance 追加=run-to-runとは独立の決定論的な第三の床。実効床=max(run-to-run,batch-invariance,数値検出限界)。GPU固有でなくCPU/TPUでも。クロスベンダーは「タイル違い=実効バッチ違い」で個別決定論的でも発散。(2)FPノイズは構造的(相関)で独立ガウスでない(arXiv:2511.00025)→calibrationの系統(RMS比)検出が必要である根拠を外部裏づけ。docstring/SOURCES/PERSPECTIVE-nondeterminismに出典明記。104テスト/verify42/ruff clean。取り込まなかった: NVIDIA Tensor Core bit-accurateモデル(arXiv:2512.07004)=実機/HW詳細要・lowering向けで将来。DASH決定論的attention(arXiv:2601.21824)=訓練向け。検証はsearch要約ベース(WebFetch不可)である点を明記。
[2026-06-16] /loop おまかせ: batch-invariance床を比較経路に実合流(取り込みの完了)。前回追加したmeasure_batch_varianceが未使用だった→compare_stable(batch_floor=)で実効床=max(run-to-run,batch_floor)・batch律速時にWARN(バッチ不変カーネルが要る)。audit_cross_vendor(run_batch=,batch_tiles=)でbatch-invariance床を実測し床に合流。実機ではrun_batchにバッチサイズ違いのforwardを渡すだけ。106テスト/verify42/ruff clean。これで2025研究の取り込みが「定義+実測+判定への合流」まで一貫。
[2026-06-16] /loop おまかせ: SOCRATIC Q18/Q20修正(取り込んだarXiv:2511.00025の操作化)。flip-bound P(margin<2δ)はδを全方向乱雑と仮定していたが、FPノイズは構造的(相関)。系統成分のうちper-sampleのアフィン(スケールα・切片c, b≈α·a+c)はargmaxを保存するのでフリップを起こさない。実証: 純スケール×1.5/一様シフトはflip=0%だがtotal_δベースboundは97%(過大)、残差(α,c最小二乗除去)ベースboundは0%(正確)。混合もtotal97%→残差26%(上界かつ正確)。実装: decision.residual_divergence_rms(per-sampleアフィンfit残差)・decompose_divergence(systematic_frac)・compare_decisionsのpredicted_boundを残差ベースに+systematic_frac報告+「数値大でも系統的ならタスク等価」をINFO。calibration(系統を数値懸念として検出)と対=同じ系統成分が数値では要注意でもタスクでは無害でありうる。既存テスト(systematicなし)は残差≈totalで不変・緑。108テスト/verify43/ruff clean。残P1: torch backend静的audit差込(Q23/26)・equivalenceをreport基盤へ(Q44/47)。
[2026-06-16] /loop おまかせ: SOCRATIC Q44/Q47修正(equivalenceを共通Risk基盤へ)。Q23/Q26(torch backend静的audit)はtorch未導入で実テスト不可ゆえ見送り(未検証統合は出さない=主張と実装の一致)。代わりに完全CPU検証可能なQ44/Q47を実施。EquivalenceReportは等価判定=スカラ計量(所見リスト型でない)ゆえFindingReportは継承せず(理由docstring明記)、共通の判定インターフェースrisk/max_risk/ok(equivalent→OK/divergent→BLOCK)を付与し第一級レポート化。これで全レポート(portability/envelope/calibration/decision/feasibility/equivalence)がmax_riskを持つ統一IF。audit_runtimeは3状態attribute()を使うので不変。109テスト/verify44/ruff clean。残: Q23/Q26(torch要・product価値最大だが要torch環境)・Q35/Q36(property test/calibration corpus ROC)・Q12等(設計/実機)。
[2026-06-16] /loop おまかせ: SOCRATIC Q35修正(property-based test)。固定seed単発だった数値主張を、多数ランダム入力で性質検証。test_properties.py: 10性質×200試行、ゼロ依存(numpy+ループ・hypothesis不使用=ゼロ依存ethos堅持)。性質: derive_tolerance K単調・detectability_floor K単調&scale非依存・residual≤total・flip_rateスケール不変・残差bound上界(系統+乱雑混合でも)・アフィン系統(α>0,シフト)は無フリップ・systematic_divergenceがα-1復元・empirical_cond正の和~1/符号付き相殺・attribute領域・envelope overflow。全性質×200試行PASS。run.py統合。119検査/verify44/ruff clean。残: Q36(calibration corpus ROC拡充)・Q23/26(torch要)・Q48-50(多seed統計/実機)。高価値CPU作業はほぼ完了。
[2026-06-16] /loop おまかせ: SOCRATIC Q36修正(calibration corpus→ROC)。9ケース合成corpusの「TRUSTWORTHY=偽OK0」は統計的に弱い→roc_sweep(strengths,K,seeds)でバグ強度を連続掃引し偽OK率を測定。結果: max_abs単独は一様スケールをrtolが吸収するため0.1%〜10%の全域で偽OK 20/20、合成判定(max_abs+系統)は系統閾値(safety·u≈0.2%)超で偽OK=0(0.5%以上で全捕捉)、0.1%(閾値未満)は合成でも見逃す=正直な残存盲点。テストで両側固定(閾値超→偽OK0/閾値未満→偽OK>0)。これでcalibrationの主張がseed単発でなく強度×多seed掃引で裏づけ。verify45/ruff clean。これでSOCRATIC 50のCPU可能P0/P1(Q8,Q9/10,Q18/20,Q35,Q36,Q44/47)全完了。残りはQ23/26(torch要)・Q48-50(実機/多seed統計)=環境制約。高価値CPU作業は完了。
[2026-06-16] /loop おまかせ: SOCRATIC Q23/Q25/Q26修正(torch backendが検証を先に届ける)。前回torch未導入で見送ったが、FX→op写像(substance)はduck-typedでtorch不要にCPUテスト可能と再評価。tsugi_torch/fxbridge.py: fx_to_graph_ops(gm)=aten op名(addmm/bmm/matmul/softmax/mean/exp/rsqrt/elementwise)をpropagation GraphOpへ写像(node.metaのtensor_meta.shapeからmatmul Kを取得)・audit_fx(gm)=FXグラフにpropagate走らせ増幅op/モデル発散をdict返し。_tsugi_compileがaudit_fxを呼びwarn("verification-only (no codegen yet)")してからeager素通し=楔の早期価値(codegen前でも検証を届ける)。Q25(ユーザー誤解防止のwarn)も同時解消。テストはstand-in FXグラフ(duck-typed _GM/_Node)で写像とaudit_fxを検証(transformerブロック模擬: addmm/bmm/softmax/mean/gelu→matmul×3/softmax/reduce/scale×2、placeholder/get_attr/output除外、K=512/2048をshape metaから)。実torch.fx結線はtorch環境要(本環境では未検証=主張と実装の一致を明記)。125関数/verify47/ruff clean。これでSOCRATIC 50のCPU可能項目(Q8,9/10,18/20,23/25/26,35,36,44/47)ほぼ全完了。残=Q24(torch実環境テスト)・Q48-50(実機/多seed統計)・GPU codegen本体=要torch/GPU。
[2026-06-16] /loop おまかせ: SOCRATIC Q1/Q2修正(safety定数の単一情報源化)。自分のSOCRATIC監査でP0と記したが未着手だった項目。最重要定数safety=4.0(全許容・検出限界・増幅をスケール)がtolerance/calibration/propagationの5箇所に重複し変更時の不整合リスク→tsugi/constants.py新設、SAFETY=4.0に集約し各既定がそれを参照(DRY)。docstringに根拠明記: GEMM f16量子化誤差は累積で√K·u·scaleにランダムウォーク的拡大(中心極限)、SAFETYはその1σ見積りに掛ける~4σヘッドルーム(真の発散捕捉/正当な順序差での偽BLOCK回避)、実機noiseで校正すべき初期値。挙動不変(SAFETY=4.0)。test_safety_is_single_source追加・verify不変条件1件。126関数/verify48/ruff clean。残P0: Q45(when改名)・Q37(SKIP件数表示)。残P1/P2: Q3(dtype別safety考察)・Q11/12(propagation DAG)・Q39-41(deps/version/CI)・Q48-50(実機)。
[2026-06-16] /loop おまかせ: SOCRATIC Q45修正(AuditPhase.when改名)。when="static"/"runtime"が意味二重定義だった(audit_runtimeは実データ実行層をstaticと付ける=「verdictに算入」の意でstaticを流用)。→ "decided"(今verdictに算入)/"pending"(実機データ待ち)に改名し名前と意味を一致。decided_phasesプロパティ新設(static_phasesは後方互換エイリアス保持)。max_risk/to_textタグ更新。audit.py全AuditPhase呼び出し・test_audit・verifyのwhen参照を更新。portcheckの[要実機データ]タグ(pending)も正常。挙動不変。126関数/verify48/ruff clean。残P0: Q37(SKIP件数表示)のみ。残: Q3/Q11/12/Q39-41/Q48-50(設計/deps/実機)。SOCRATIC 50のCPU可能項目はQ37を除き実質完了。
[2026-06-16] /loop おまかせ: SOCRATIC Q37修正(テストサマリ正直化)。run.py末尾にSUMMARY追加: CPU suites PASS件数 + SKIPPED(要hardware)件数の列挙 + 「GPU経路は未検証・緑はCPU検証可能範囲のみ」注記。緑(rc=0)が「全部検証済み」と誤読されるのを防ぐ(主張と実装の一致の運用面)。これでSOCRATIC 50のCPU可能P0/P1全完了(Q8,9/10,18/20,23/25/26,35,36,37,44/47,45,Q1/2)。残りはすべて環境制約(torch:Q24/GPU:Q48-50/codegen)か設計重め(Q11/12 propagation DAG)・方針物(Q39-41 deps/version/CI honesty)。126関数/verify48/ruff clean。
[2026-06-16] /loop おまかせ: SOCRATIC Q49修正(外れ値頑健なノイズ床)。measure_noise_floor/measure_batch_varianceのspread(max-min)は測定グリッチ1個で過大評価(実証~4万倍膨張)→偽BLOCK化リスク。_spread_stats共通化しspread_robust(10-90パーセンタイル幅・上下10%トリム=単発外れ値を除外)を併記。compare_stable(robust=True)で選択(既定はspread=保守的・後方互換)。実証: seed0だけ外れ値の16runでspread_robustはspreadの<1%。127関数/verify49/ruff clean。SOCRATIC 50のCPU可能項目ほぼ全完了(残: Q3 dtype別safety考察=分析/Q11/12 propagation DAG=設計重/Q24 torch実環境/Q39-41 deps&version&CI policy/Q48,50 実機統計)。
[2026-06-16] /loop 実装: SOCRATIC Q39/Q40/Q41修正(packaging/version/CI honesty)。Q39着手で更にpackaging欠陥発覚: pyprojectがcore tsugiパッケージをbuild対象外(include=tsugi_torch*のみ)・numpy依存も未宣言だった→include=["tsugi*"](両パッケージ捕捉)・dependencies=["numpy>=1.22"]・torchをoptional-dependenciesへ。Q40: version 0.1.0→0.2.0(pyproject+__version__)・CHANGELOGに[0.2.0]2026-06-16見出し・0.x API未凍結明記。Q41: CONTRIBUTINGに「CIについて(正直な現状)」節=GitHub Actions無効(.github除外)・ローカルrun.py(SUMMARY付)+verify.pyがCI代替・protected-mainの「CI全通過」を実態修正・stale test件数更新。verify49維持/127関数/ruff clean。SOCRATIC 50完了状況: CPU可能項目すべて実施。残=Q3(dtype別safety=分析のみ)・Q11/12(propagation DAG=要設計)・Q24(torch実環境)・Q48/50(実機統計)・Q27-32等(設計/将来)。実装可能な改善は出尽くし。
[2026-06-16] /loop 実装: propagation 残差トポロジ対応(Q11/Q12)。前イテレーションで「残差は検証できない」と一度却下したが、却下理由が不適切なプロトタイプ(正規化なしで残差ストリーム爆発)だったと判明。pre-norm(y=x+W·rmsnorm(x))で再検証すると残差の相対発散が平坦チェーンより一貫して低い(深さ48で2.7x低・両者sub-linear)ことをnumpy実測で確認。実装: GraphOp.residual=True、propagateで残差opは δ_out=sqrt(δ_in²+(amp·local)²)(skip接続がδ_inを再増幅せず、ブロック寄与のみrandom-walk加算=希釈、~√L)、平坦は従来通りδ_out=amp·δ_in+local(線形累積)。深さ24でmodel_divergence比0.20、単一ブロックでは差なし(純粋な深さ効果)。test_propagation: モデルレベル(残差<平坦*0.5)+numpy pre-norm実測(残差<平坦)の両方で検証。_graph_ops(IR自動抽出)は残差未検出=非残差(保守的過大評価)既定。一次近似であることを明記(既存propagationと同じ標準)。129関数/verify50/ruff clean。教訓: 「検証不能」と却下する前にプロトタイプの妥当性自体を疑う。
[2026-06-16] 長所短所棚卸し+実装: 棚卸しで最大の盲点=直前追加のresidualもだが、より大きいのは「audit_fxがmodel_divergence(相対)を出すがユーザーに見えるタスク指標(フリップ率)へ翻訳していない」点。decision.flip_bound_from_divergenceは存在するのに製品経路(torch backend)で未接続だった。実装: audit_fx(gm, ref_logits=)でmodel_divergence→task_flip_bound翻訳。backendはgm.forward(example_inputs)の出力を代表logitとしてbest-effort取得しtask_flip_bound≤Y%をwarn(model_divergenceでなくユーザーに見える予測フリップ率を届ける)=propagation→decisionをFX経路で接続(静的グラフ→タスク影響の全パイプライン)。test_fxbridge: ref_logits無→None/有→[0,1]確率。verify不変条件1件。130関数/verify51/ruff clean。
長所: 検証層が包括的(8視点+メタ+基盤+翻訳+統合)・honesty厳守・numpy oracle検証・property/ROC/論文取込・残差トポロジ。短所/残改善点: (a)residualもtask翻訳もauto-extraction(_graph_ops/FX)で残差は未検出=保守的(要dataflow解析・将来) (b)_graph_opsとfx_to_graph_opsが並行extractor=op-kind写像にDRY余地 (c)audit.py 306行で最大・分割余地 (d)実機検証は依然未(torch/GPU要)。
